var mschema = require('../');

var port = {
  "type": "string",
  "enum": ["usb", "vga", "displayport", "firewire"]
}

var schema = {
  ports: [port]
};

var obj = {
  ports: ["usb", "displayport"]
}

var re = mschema.validate(obj, schema);

console.log(re);