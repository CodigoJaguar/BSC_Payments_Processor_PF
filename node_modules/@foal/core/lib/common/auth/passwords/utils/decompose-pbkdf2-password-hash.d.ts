/// <reference types="node" />
export declare function decomposePbkdf2PasswordHash(passwordHash: string): {
    digestAlgorithm: 'sha256';
    iterations: number;
    salt: Buffer;
    derivedKey: Buffer;
    keyLength: number;
};
