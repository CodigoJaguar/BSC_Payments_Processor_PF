"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertErrorToResponse = void 0;
const common_1 = require("../../common");
const config_1 = require("../config");
async function convertErrorToResponse(error, ctx, appController, log = console.error) {
    if (config_1.Config.get('settings.logErrors', 'boolean', true)) {
        log(error.stack);
    }
    if (appController.handleError) {
        try {
            return await appController.handleError(error, ctx);
        }
        catch (error2) {
            return (0, common_1.renderError)(error2, ctx);
        }
    }
    return (0, common_1.renderError)(error, ctx);
}
exports.convertErrorToResponse = convertErrorToResponse;
