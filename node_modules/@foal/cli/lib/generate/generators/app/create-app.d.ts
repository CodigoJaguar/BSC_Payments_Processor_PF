export declare function createApp({ name, autoInstall, initRepo, mongodb, yaml }: {
    name: string;
    autoInstall?: boolean;
    initRepo?: boolean;
    mongodb?: boolean;
    yaml?: boolean;
}): Promise<void>;
